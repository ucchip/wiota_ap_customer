menu "wiota APP"

config WIOTA_API_TEST
    bool "uc wiota api test, default close(WIOTA_API_TEST)"
    default y

config GATEWAY_MODE_SUPPORT
    bool "release version must close, single gateway used(GATEWAY_MODE_SUPPORT)"
    default y

if RT_USING_WDT
    config _WATCHDOG_APP_
        bool "open wdt, default close(_WATCHDOG_APP_)"
        default n
endif

endmenu
